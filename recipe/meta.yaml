{% set name = "mssql-django" %}
{% set version = "1.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mssql_django-{{ version }}.tar.gz
  sha256: 8bb18ac2da4467eae683e26ece257decdd0a20394a40bf86c0cb07819a678497

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - django >=3.2,<6.1
    - pyodbc >=3.0
    - pytz

test:
  imports:
    - mssql
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/microsoft/mssql-django
  summary: Django backend for Microsoft SQL Server
  license: BSD-3-Clause
  license_file:
    - LICENSE.txt
    - NOTICE.md

extra:
  recipe-maintainers:
    - rxm7706
    - cshaley
